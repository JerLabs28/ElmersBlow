'use strict';

var cst = require('./cst.js');
var lexer = require('./lexer.js');

function includesToken(list, type) {
    for (let i = 0; i < list.length; ++i)
        if (list[i].type === type)
            return true;
    return false;
}
function findNonEmptyIndex(list) {
    for (let i = 0; i < list.length; ++i) {
        switch (list[i].type) {
            case 'space':
            case 'comment':
            case 'newline':
                break;
            default:
                return i;
        }
    }
    return -1;
}
function isFlowToken(token) {
    switch (token?.type) {
        case 'alias':
        case 'scalar':
        case 'single-quoted-scalar':
        case 'double-quoted-scalar':
        case 'flow-collection':
            return true;
        default:
            return false;
    }
}
function getPrevProps(parent) {
    switch (parent.type) {
        case 'document':
            return parent.start;
        case 'block-map': {
            const it = parent.items[parent.items.length - 1];
            return it.sep ?? it.start;
        }
        case 'block-seq':
            return parent.items[parent.items.length - 1].start;
        /* istanbul ignore next should not happen */
        default:
            return [];
    }
}
/** Note: May modify input array */
function getFirstKeyStartProps(prev) {
    if (prev.length === 0)
        return [];
    let i = prev.length;
    loop: while (--i >= 0) {
        switch (prev[i].type) {
            case 'doc-start':
            case 'explicit-key-ind':
            case 'map-value-ind':
            case 'seq-item-ind':
            case 'newline':
                break loop;
        }
    }
    while (prev[++i]?.type === 'space') {
        /* loop */
    }
    return prev.splice(i, prev.length);
}
function fixFlowSeqItems(fc) {
    if (fc.start.type === 'flow-seq-start') {
        for (const it of fc.items) {
            if (it.sep &&
                !it.value &&
                !includesToken(it.start, 'explicit-key-ind') &&
                !includesToken(it.sep, 'map-value-ind')) {
                if (it.key)
                    it.value = it.key;
                delete it.key;
                if (isFlowToken(it.value)) {
                    if (it.value.end)
                        Array.prototype.push.apply(it.value.end, it.sep);
                    else
                        it.value.end = it.sep;
                }
                else
                    Array.prototype.push.apply(it.start, it.sep);
                delete it.sep;
            }
        }
    }
}
/**
 * A YAML concrete syntax tree (CST) parser
 *
 * ```ts
 * const src: string = ...
 * for (const token of new Parser().parse(src)) {
 *   // token: Token
 * }
 * ```
 *
 * To use the parser with a user-provided lexer:
 *
 * ```ts
 * function* parse(source: string, lexer: Lexer) {
 *   const parser = new Parser()
 *   for (const lexeme of lexer.lex(source))
 *     yield* parser.next(lexeme)
 *   yield* parser.end()
 * }
 *
 * const src: string = ...
 * const lexer = new Lexer()
 * for (const token of parse(src, lexer)) {
 *   // token: Token
 * }
 * ```
 */
class Parser {
    /**
     * @param onNewLine - If defined, called separately with the start position of
     *   each new line (in `parse()`, including the start of input).
     */
    constructor(onNewLine) {
        /** If true, space and sequence indicators count as indentation */
        this.atNewLine = true;
        /** If true, next token is a scalar value */
        this.atScalar = false;
        /** Current indentation level */
        this.indent = 0;
        /** Current offset since the start of parsing */
        this.offset = 0;
        /** On the same line with a block map key */
        this.onKeyLine = false;
        /** Top indicates the node that's currently being built */
        this.stack = [];
        /** The source of the current token, set in parse() */
        this.source = '';
        /** The type of the current token, set in parse() */
        this.type = '';
        // Must be defined after `next()`
        this.lexer = new lexer.Lexer();
        this.onNewLine = onNewLine;
    }
    /**
     * Parse `source` as a YAML stream.
     * If `incomplete`, a part of the last line may be left as a buffer for the next call.
     *
     * Errors are not thrown, but yielded as `{ type: 'error', message }` tokens.
     *
     * @returns A generator of tokens representing each directive, document, and other structure.
     */
    *parse(source, incomplete = false) {
        if (this.onNewLine && this.offset === 0)
            this.onNewLine(0);
        for (const lexeme of this.lexer.lex(source, incomplete))
            yield* this.next(lexeme);
        if (!incomplete)
            yield* this.end();
    }
    /**
     * Advance the parser by the `source` of one lexical token.
     */
    *next(source) {
        this.source = source;
        if (process.env.LOG_TOKENS)
            console.log('|', cst.prettyToken(source));
        if (this.atScalar) {
            this.atScalar = false;
            yield* this.step();
            this.offset += source.length;
            return;
        }
        const type = cst.tokenType(source);
        if (!type) {
            const message = `Not a YAML token: ${source}`;
            yield* this.pop({ type: 'error', offset: this.offset, message, source });
            this.offset += source.length;
        }
        else if (type === 'scalar') {
            this.atNewLine = false;
            this.atScalar = true;
            this.type = 'scalar';
        }
        else {
            this.type = type;
            yield* this.step();
            switch (type) {
                case 'newline':
                    this.atNewLine = true;
                    this.indent = 0;
                    if (this.onNewLine)
                        this.onNewLine(this.offset + source.length);
                    break;
                case 'space':
                    if (this.atNewLine && source[0] === ' ')
                        this.indent += source.length;
                    break;
                case 'explicit-key-ind':
                case 'map-value-ind':
                case 'seq-item-ind':
                    if (this.atNewLine)
                        this.indent += source.length;
                    break;
                case 'doc-mode':
                case 'flow-error-end':
                    return;
                default:
                    this.atNewLine = false;
            }
            this.offset += source.length;
        }
    }
    /** Call at end of input to push out any remaining constructions */
    *end() {
        while (this.stack.length > 0)
            yield* this.pop();
    }
    get sourceToken() {
        const st = {
            type: this.type,
            offset: this.offset,
            indent: this.indent,
            source: this.source
        };
        return st;
    }
    *step() {
        const top = this.peek(1);
        if (this.type === 'doc-end' && (!top || top.type !== 'doc-end')) {
            while (this.stack.length > 0)
                yield* this.pop();
            this.stack.push({
                type: 'doc-end',
                offset: this.offset,
                source: this.source
            });
            return;
        }
        if (!top)
            return yield* this.stream();
        switch (top.type) {
            case 'document':
                return yield* this.document(top);
            case 'alias':
            case 'scalar':
            case 'single-quoted-scalar':
            case 'double-quoted-scalar':
                return yield* this.scalar(top);
            case 'block-scalar':
                return yield* this.blockScalar(top);
            case 'block-map':
                return yield* this.blockMap(top);
            case 'block-seq':
                return yield* this.blockSequence(top);
            case 'flow-collection':
                return yield* this.flowCollection(top);
            case 'doc-end':
                return yield* this.documentEnd(top);
        }
        /* istanbul ignore next should not happen */
        yield* this.pop();
    }
    peek(n) {
        return this.stack[this.stack.length - n];
    }
    *pop(error) {
        const token = error ?? this.stack.pop();
        /* istanbul ignore if should not happen */
        if (!token) {
            const message = 'Tried to pop an empty stack';
            yield { type: 'error', offset: this.offset, source: '', message };
        }
        else if (this.stack.length === 0) {
            yield token;
        }
        else {
            const top = this.peek(1);
            if (token.type === 'block-scalar') {
                // Block scalars use their parent rather than header indent
                token.indent = 'indent' in top ? top.indent : 0;
            }
            else if (token.type === 'flow-collection' && top.type === 'document') {
                // Ignore all indent for top-level flow collections
                token.indent = 0;
            }
            if (token.type === 'flow-collection')
                fixFlowSeqItems(token);
            switch (top.type) {
                case 'document':
                    top.value = token;
                    break;
                case 'block-scalar':
                    top.props.push(token); // error
                    break;
                case 'block-map': {
                    const it = top.items[top.items.length - 1];
                    if (it.value) {
                        top.items.push({ start: [], key: token, sep: [] });
                        this.onKeyLine = true;
                        return;
                    }
                    else if (it.sep) {
                        it.value = token;
                    }
                    else {
                        Object.assign(it, { key: token, sep: [] });
                        this.onKeyLine = !includesToken(it.start, 'explicit-key-ind');
                        return;
                    }
                    break;
                }
                case 'block-seq': {
                    const it = top.items[top.items.length - 1];
                    if (it.value)
                        top.items.push({ start: [], value: token });
                    else
                        it.value = token;
                    break;
                }
                case 'flow-collection': {
                    const it = top.items[top.items.length - 1];
                    if (!it || it.value)
                        top.items.push({ start: [], key: token, sep: [] });
                    else if (it.sep)
                        it.value = token;
                    else
                        Object.assign(it, { key: token, sep: [] });
                    return;
                }
                /* istanbul ignore next should not happen */
                default:
                    yield* this.pop();
                    yield* this.pop(token);
            }
            if ((top.type === 'document' ||
                top.type === 'block-map' ||
                top.type === 'block-seq') &&
                (token.type === 'block-map' || token.type === 'block-seq')) {
                const last = token.items[token.items.length - 1];
                if (last &&
                    !last.sep &&
                    !last.value &&
                    last.start.length > 0 &&
                    findNonEmptyIndex(last.start) === -1 &&
                    (token.indent === 0 ||
                        last.start.every(st => st.type !== 'comment' || st.indent < token.indent))) {
                    if (top.type === 'document')
                        top.end = last.start;
                    else
                        top.items.push({ start: last.start });
                    token.items.splice(-1, 1);
                }
            }
        }
    }
    *stream() {
        switch (this.type) {
            case 'directive-line':
                yield { type: 'directive', offset: this.offset, source: this.source };
                return;
            case 'byte-order-mark':
            case 'space':
            case 'comment':
            case 'newline':
                yield this.sourceToken;
                return;
            case 'doc-mode':
            case 'doc-start': {
                const doc = {
                    type: 'document',
                    offset: this.offset,
                    start: []
                };
                if (this.type === 'doc-start')
                    doc.start.push(this.sourceToken);
                this.stack.push(doc);
                return;
            }
        }
        yield {
            type: 'error',
            offset: this.offset,
            message: `Unexpected ${this.type} token in YAML stream`,
            source: this.source
        };
    }
    *document(doc) {
        if (doc.value)
            return yield* this.lineEnd(doc);
        switch (this.type) {
            case 'doc-start': {
                if (findNonEmptyIndex(doc.start) !== -1) {
                    yield* this.pop();
                    yield* this.step();
                }
                else
                    doc.start.push(this.sourceToken);
                return;
            }
            case 'anchor':
            case 'tag':
            case 'space':
            case 'comment':
            case 'newline':
                doc.start.push(this.sourceToken);
                return;
        }
        const bv = this.startBlockValue(doc);
        if (bv)
            this.stack.push(bv);
        else {
            yield {
                type: 'error',
                offset: this.offset,
                message: `Unexpected ${this.type} token in YAML document`,
                source: this.source
            };
        }
    }
    *scalar(scalar) {
        if (this.type === 'map-value-ind') {
            const prev = getPrevProps(this.peek(2));
            const start = getFirstKeyStartProps(prev);
            let sep;
            if (scalar.end) {
                sep = scalar.end;
                sep.push(this.sourceToken);
                delete scalar.end;
            }
            else
                sep = [this.sourceToken];
            const map = {
                type: 'block-map',
                offset: scalar.offset,
                indent: scalar.indent,
                items: [{ start, key: scalar, sep }]
            };
            this.onKeyLine = true;
            this.stack[this.stack.length - 1] = map;
        }
        else
            yield* this.lineEnd(scalar);
    }
    *blockScalar(scalar) {
        switch (this.type) {
            case 'space':
            case 'comment':
            case 'newline':
                scalar.props.push(this.sourceToken);
                return;
            case 'scalar':
                scalar.source = this.source;
                // block-scalar source includes trailing newline
                this.atNewLine = true;
                this.indent = 0;
                if (this.onNewLine) {
                    let nl = this.source.indexOf('\n') + 1;
                    while (nl !== 0) {
                        this.onNewLine(this.offset + nl);
                        nl = this.source.indexOf('\n', nl) + 1;
                    }
                }
                yield* this.pop();
                break;
            /* istanbul ignore next should not happen */
            default:
                yield* this.pop();
                yield* this.step();
        }
    }
    *blockMap(map) {
        const it = map.items[map.items.length - 1];
        // it.sep is true-ish if pair already has key or : separator
        switch (this.type) {
            case 'newline':
                this.onKeyLine = false;
                if (it.value) {
                    const end = 'end' in it.value ? it.value.end : undefined;
                    const last = Array.isArray(end) ? end[end.length - 1] : undefined;
                    if (last?.type === 'comment')
                        end?.push(this.sourceToken);
                    else
                        map.items.push({ start: [this.sourceToken] });
                }
                else if (it.sep) {
                    it.sep.push(this.sourceToken);
                }
                else {
                    it.start.push(this.sourceToken);
                }
                return;
            case 'space':
            case 'comment':
                if (it.value) {
                    map.items.push({ start: [this.sourceToken] });
                }
                else if (it.sep) {
                    it.sep.push(this.sourceToken);
                }
                else {
                    if (this.atIndentedComment(it.start, map.indent)) {
                        const prev = map.items[map.items.length - 2];
                        const end = prev?.value?.end;
                        if (Array.isArray(end)) {
                            Array.prototype.push.apply(end, it.start);
                            end.push(this.sourceToken);
                            map.items.pop();
                            return;
                        }
                    }
                    it.start.push(this.sourceToken);
                }
                return;
        }
        if (this.indent >= map.indent) {
            const atNextItem = !this.onKeyLine &&
                this.indent === map.indent &&
                it.sep &&
                this.type !== 'seq-item-ind';
            // For empty nodes, assign newline-separated not indented empty tokens to following node
            let start = [];
            if (atNextItem && it.sep && !it.value) {
                const nl = [];
                for (let i = 0; i < it.sep.length; ++i) {
                    const st = it.sep[i];
                    switch (st.type) {
                        case 'newline':
                            nl.push(i);
                            break;
                        case 'space':
                            break;
                        case 'comment':
                            if (st.indent > map.indent)
                                nl.length = 0;
                            break;
                        default:
                            nl.length = 0;
                    }
                }
                if (nl.length >= 2)
                    start = it.sep.splice(nl[1]);
            }
            switch (this.type) {
                case 'anchor':
                case 'tag':
                    if (atNextItem || it.value) {
                        start.push(this.sourceToken);
                        map.items.push({ start });
                        this.onKeyLine = true;
                    }
                    else if (it.sep) {
                        it.sep.push(this.sourceToken);
                    }
                    else {
                        it.start.push(this.sourceToken);
                    }
                    return;
                case 'explicit-key-ind':
                    if (!it.sep && !includesToken(it.start, 'explicit-key-ind')) {
                        it.start.push(this.sourceToken);
                    }
                    else if (atNextItem || it.value) {
                        start.push(this.sourceToken);
                        map.items.push({ start });
                    }
                    else {
                        this.stack.push({
                            type: 'block-map',
                            offset: this.offset,
                            indent: this.indent,
                            items: [{ start: [this.sourceToken] }]
                        });
                    }
                    this.onKeyLine = true;
                    return;
                case 'map-value-ind':
                    if (includesToken(it.start, 'explicit-key-ind')) {
                        if (!it.sep) {
                            if (includesToken(it.start, 'newline')) {
                                Object.assign(it, { key: null, sep: [this.sourceToken] });
                            }
                            else {
                                const start = getFirstKeyStartProps(it.start);
                                this.stack.push({
                                    typeINDX( 	 ¯ğ          (   x
  è       g                     Të    p Z     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙ0      .              N A D F F 6 ~ 1 . P N G p a g Wë    p Z     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙh      g              N A E 1 3 B ~ 1 . P N G p a g ë    p Z     Í    ÜÍ¹¦JóÙ£,Ê¨¢ƒÕÜÍ¹¦JóÙÜÍ¹¦JóÙ       5              N A E 3 B 8 ~ 1 . P N G p a g xë    p Z     Í    •}Ê¦JóÙ³â¨¢ƒÕ•}Ê¦JóÙ•}Ê¦JóÙ                      N A E 6 C 7 ~ 1 . P N G p a  ;ë    p Z     Í    ›ôÀ¦JóÙB¶Ó¨¢ƒÕ›ôÀ¦JóÙ›ôÀ¦JóÙ       k              N A E 8 5 1 ~ 1 . P N G p a g Zë    p Z     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙh      g              N A E 9 2 F ~ 1 . P N G p a g ë    p Z     Í    ™k·¦JóÙßgÅ¨¢ƒÕ™k·¦JóÙ™k·¦JóÙ                     N A E 9 A 4 ~ 1 . P N G p a g ë    p Z     Í    ÜÍ¹¦JóÙ£,Ê¨¢ƒÕÜÍ¹¦JóÙÜÍ¹¦JóÙ       „              N A E A 7 3 ~ 1 . P N G p a g gë    p Z     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ      Ä              N A E E 2 2 ~ 1 . P N G p a g ıê    p Z     Í    µ¦JóÙoÃ¨¢ƒÕµ¦JóÙµ¦JóÙ       Ò	              N A F 0 B E ~ 1 . P N G p a g ‹ë    p Z     Í    ÚßÌ¦JóÙgä¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙP      J              N A F 0 D 9 ~ 1 . P N G p a g Që    p Z     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙ0      .              N A F 0 F F ~ 1 . P N G p a g ’ë    p Z     Í    ÚßÌ¦JóÙxÉæ¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙ       œ              N A F 9 2 B ~ 1 . P N G p a g Oë    p Z    Í    ÙVÃ¦JóÙ{Ø¨¢ƒÕÙVÃ¦JóÙÙVÃ¦JóÙ                     N A F 9 9 D ~ 1 . P N G p a g —ë    p Z     Í    BÏ¦JóÙxÉæ¨¢ƒÕBÏ¦JóÙBÏ¦JóÙ                     N A F B 2 B ~ 1 . P N G p a g +ë    p Z     Í    ’¾¦JóÙÓSÑ¨¢ƒÕ’¾¦JóÙ’¾¦JóÙ€      y              N A F C 0 F ~ 1 . P N G p a g ë    p Z     Í    ÜÍ¹¦JóÙ£,Ê¨¢ƒÕÜÍ¹¦JóÙÜÍ¹¦JóÙ                     N A F C 7 A ~ 1 . P N G p a g •ë    p Z     Í    BÏ¦JóÙxÉæ¨¢ƒÕBÏ¦JóÙBÏ¦JóÙ                   N A F F F A ~ 1 . P N G p a g ôê    € j     Í    h«¦JóÙŒ¾-@“¬Õâ­¦JóÙh«¦JóÙ       —              n a r r a t o r h o m e p a g e . x b f       õê    € n     Í    â­¦JóÙ‡×,•¬ÕØ¨²¦JóÙØ¨²¦JóÙ        ü             n a r r a t o r q u i c k s t a r t . e x e   öê      Œ     Í    Ø¨²¦JóÙ	£À¨¢ƒÕµ¦JóÙµ¦JóÙ       Å              %n a r r a t o r u w p s p l a s h s c r e e n . s c a l e - 1 0 0 . p n g JóÙ÷ê    À ª     Í    µ¦JóÙ	£À¨¢ƒÕµ¦JóÙµ¦JóÙ      Å              4n a r r a t o r u w p s p l a s h s c r e e n . s c a l e - 1 0 0 _ c o n t r a s t - b l a c k . p n g                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  INDX( 	 âÕğ          (   p  è         .  . o p Õ        \ë    È ¸     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙ       d              ;n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 4 8 _ c o n t r a s t - w h i t e . p n g ^ë    ° š     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ¨      ¨              ,n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 0 . p n g f o r ]ë    Ğ ¼     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ¨      ¨             =n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 0 _ a l t f o r m - u n p l a t e d . p n g T ~ _ë    ğ Ú     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ¨      ¨              Ln a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 0 _ a l t f o r m - u n p l a t e d _ c o n t r a s t - b l a c k . p n g Z     aë    ğ Ú     Í    \È¦JóÙ?¢ß¨¢ƒÕ\È¦JóÙ\È¦JóÙ       š              Ln a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o  t a r g e t s i z e - 6 0 _ a l t f o r m - u n p l a t e d _ c o n t r a s t - w h i t e . p n g ×,•¬Õ`ë    È ¸     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ¨      ¨              ;n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 0 _ c o n t r a s t - b l a c k . p n g bë    È ¸     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ       š              ;n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 0 _ c o n t r a s t - w h i t e . p n g dë    ° š     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙÀ      »              ,n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 4 . p n g f o r cë    Ğ ¼     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙÀ      »              =n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 4 _ a l t f o r m - u n p l a t e d . p n g T ~               Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙÀ      »              Ln a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o  t a r g e t s i z e - 6 4 _ a l t f o r m - u n p l a t e d _ c o n t r a s t - b l a c k . p n g Z     gë    ğ Ú     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙ       Ä              Ln a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 4 _ a l t f o r m - u n p l a t e d _ c o n t r a s t - w h i t e . p n g ×,•¬Õfë    È ¸     Í    \È¦JóÙ?¢ß¨¢ƒÕ\È¦JóÙ\È¦JóÙÀ      »              ;n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 4 _ c  n t r a s t - b l a c k . p n g hë    È ¸     Í    \È¦JóÙ?¢ß¨¢ƒÕ\È¦JóÙ\È¦JóÙ       Ä              ;n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 6 4 _ c o n t r a s t - w h i t e . p n g jë    ° š     Í    \È¦JóÙ?¢ß¨¢ƒÕ\È¦JóÙ\È¦JóÙ       ı              ,n a r r a t o r u w p s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 7 2 . p n g f o r ië    ğ Ú     Í    \È¦JóÙ?¢ß¨¢ƒÕ\È¦JóÙ\È¦JóÙ       ı              Ln a r r a t o r u w  s q u a r e 4 4 x 4 4 l o g o . t a r g e t s i z e - 7 2 _ a l t f o r m - u n p l a t e d _ c o n t r a s t - b l a c k . p n g Z     õê    p Z     Í    â­¦JóÙ‡×,•¬ÕØ¨²¦JóÙØ¨²¦JóÙ        ü             N A R R A T ~ 1 . E X E       öê    p Z     Í    Ø¨²¦JóÙ	£À¨¢ƒÕµ¦JóÙµ¦JóÙ       Å              N A R R A T ~ 1 . P N G       ôê    p Z     Í    h«¦JóÙŒ¾-@“¬Õâ­¦JóÙh«¦JóÙ       —              N A R R A T ~ 1 . X B F       ÷ê    p Z     Í    µ¦JóÙ	£À¨¢ƒ µ¦JóÙµ¦JóÙ       Å              N A R R A T ~ 2 . P N G       øê    p Z     Í    µ¦JóÙoÃ¨¢ƒÕµ¦JóÙµ¦JóÙ       ÷              N A R R A T ~ 3 . P N G       ùê    p Z     Í    µ¦JóÙoÃ¨¢ƒÕµ¦JóÙµ¦JóÙ       ğ              N A R R A T ~ 4 . P N G                                                                                                                                                                                                                            INDX( 	 ø§ğ          (   ¸
  è       g       Ù            dë    p Z     Í    \È¦JóÙÔ?İ¨¢ƒÕ\È¦JóÙ\È¦JóÙÀ      »              N A 2 1 2 2 ~ 1 . P N G p a g ë    p Z     Í    ÚßÌ¦JóÙ³â¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙ       E              N A 2 1 3 D ~ 1 . P N G p a g Së    p Z     Í    ¹Å¦JóÙnİÚ¨¢ƒÕ¹Å¦JóÙ¹Å¦JóÙ0      .              N A 2 1 E A ~ 1 . P N G p a g –ë    p Z     Í    BÏ¦JóÙxÉæ¨¢ƒÕBÏ¦JóÙBÏ¦JóÙ       ;              N A 2 2 0 C ~ 1 . P N G p a  ë    p Z     Í    0¼¦JóÙ
Ì¨¢ƒÕ0¼¦JóÙ0¼¦JóÙˆ      ‡              N A 2 2 9 1 ~ 1 . P N G p a g Më    p Z     Í    ÙVÃ¦JóÙ{Ø¨¢ƒÕÙVÃ¦JóÙÙVÃ¦JóÙ                     N A 2 3 2 2 ~ 1 . P N G p a g )ë    p Z     Í    ’¾¦JóÙqñÎ¨¢ƒÕ’¾¦JóÙ’¾¦JóÙ°       ¯               N A 2 5 F 7 ~ 1 . P N G p a g ë    p Z     Í    ™k·¦JóÙßgÅ¨¢ƒÕ™k·¦JóÙ™k·¦JóÙ                     N A 2 6 F D ~ 1 . P N G p a g “ë    p Z     Í    ÚßÌ¦JóÙxÉæ¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙ      µ              N A 2 8 D B ~ 1 . P N G p a g úê    p Z     Í    µ¦JóÙoÃ¨¢ƒÕµ¦JóÙµ¦JóÙ       ğ              N A 2 C 2 4 ~ 1 . P N G p a g ë    p Z     Í    ™k·¦JóÙßgÅ¨¢ƒÕ™k·¦JóÙ™k·¦JóÙ 0      Ä(              N A 2 E 3 A ~ 1 . P N G p a g ‘ë    p Z     Í    ÚßÌ¦JóÙgä¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙ       œ              N A 2 F 4 7 ~ 1 . P N G p a g ûê    p Z     Í    µ¦JóÙoÃ¨¢ƒÕµ¦JóÙµ¦JóÙ                     N A 3 1 1 C ~ 1 . P N G p a g ë    p Z    Í    ™k·¦JóÙßgÅ¨¢ƒÕ™k·¦JóÙ™k·¦JóÙ       ñ              N A 3 3 1 B ~ 1 . P N G p a g =ë    p Z     Í    ›ôÀ¦JóÙB¶Ó¨¢ƒÕ›ôÀ¦JóÙ›ôÀ¦JóÙ       
              N A 3 4 6 E ~ 1 . P N G p a g ë    p Z     Í    0¼¦JóÙ£,Ê¨¢ƒÕ0¼¦JóÙ0¼¦JóÙ                     N A 3 6 A A ~ 1 . P N G p a g të    p Z     Í    •}Ê¦JóÙ³â¨¢ƒÕ•}Ê¦JóÙ•}Ê¦JóÙ                     N A 3 6 F 7 ~ 1 . P N G p a g që    p Z     Í    •}Ê¦JóÙ³â¨¢ƒÕ•}Ê¦JóÙ•}Ê¦JóÙ                    N A 3 B 6 4 ~ 1 . P N G p a g ë    p Z     Í    0¼¦JóÙ
Ì¨¢ƒÕ0¼¦JóÙ0¼¦JóÙˆ      ‡              N A 3 E 0 7 ~ 1 . P N G p a g ?ë    p Z     Í    ›ôÀ¦JóÙ©Ö¨¢ƒÕ›ôÀ¦JóÙ›ôÀ¦JóÙ                    N A 3 F 4 9 ~ 1 . P N G p a g 
ë    p Z     Í    ™k·¦JóÙAÊÇ¨¢ƒÕÜÍ¹¦JóÙÜÍ¹¦JóÙ                     N A 4 1 7 7 ~ 1 . P N G p a g 0ë    p Z     Í    ’¾¦JóÙ{Ø¨¢ƒÕ’¾¦JóÙ’¾¦JóÙÀ       ¹               N A 4 1 7 7 ~ 2 . P N G p a g Gë    p Z     Í    ÙVÃ¦Jó ©Ö¨¢ƒÕÙVÃ¦JóÙÙVÃ¦JóÙ                    N A 4 1 D 7 ~ 1 . P N G p a g ë    p Z     Í    ÚßÌ¦JóÙgä¨¢ƒÕÚßÌ¦JóÙÚßÌ¦JóÙx      t              N A 4 2 6 5 ~ 1 . P N G                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  {
                this.onKeyLine = true;
                const prev = getPrevProps(parent);
                const start = getFirstKeyStartProps(prev);
                return {
                    type: 'block-map',
                    offset: this.offset,
                    indent: this.indent,
                    items: [{ start, key: null, sep: [this.sourceToken] }]
                };
            }
        }
        return null;
    }
    atIndentedComment(start, indent) {
        if (this.type !== 'comment')
            return false;
        if (this.indent <= indent)
            return false;
        return start.every(st => st.type === 'newline' || st.type === 'space');
    }
    *documentEnd(docEnd) {
        if (this.type !== 'doc-mode') {
            if (docEnd.end)
                docEnd.end.push(this.sourceToken);
            else
                docEnd.end = [this.sourceToken];
            if (this.type === 'newline')
                yield* this.pop();
        }
    }
    *lineEnd(token) {
        switch (this.type) {
            case 'comma':
            case 'doc-start':
            case 'doc-end':
            case 'flow-seq-end':
            case 'flow-map-end':
            case 'map-value-ind':
                yield* this.pop();
                yield* this.step();
                break;
            case 'newline':
                this.onKeyLine = false;
            // fallthrough
            case 'space':
            case 'comment':
            default:
                // all other values are errors
                if (token.end)
                    token.end.push(this.sourceToken);
                else
                    token.end = [this.sourceToken];
                if (this.type === 'newline')
                    yield* this.pop();
        }
    }
}

exports.Parser = Parser;
